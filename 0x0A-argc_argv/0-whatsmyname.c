#include "main.h"
#include <stdio.h>

/**
 * main - print its name
 * @argc - argument counts
 * @argv - argument vector
 *
 * Return: Always zero
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[argc * 0]);

	return (0);
}
